"""Local test helpers package.

This file keeps imports such as ``tests.test_agent._fakes`` resolved to this
repository's test tree even when the active Python environment contains an
unrelated third-party package named ``tests``.
"""
